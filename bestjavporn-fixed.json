{
  "sites": [
    {
      "key": "bestjavporn",
      "name": "BestJavPorn",
      "type": 4,
      "api": "",
      "searchable": 1,
      "quickSearch": 0,
      "filterable": 1,
      "headers": {
        "Referer": "https://www.bestjavporn.com/",
        "User-Agent": "Mozilla/5.0 (Linux; Android 13; Mobile) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.0.0 Mobile Safari/537.36"
      },
      "ext": {
        "baseUrl": "https://www.bestjavporn.com",
        "inlineScript": "__JS_SPIDER__ = (() => { let config = {}; const LIST_JS = `JSON.stringify((() => { const cards = new Map(); const absolute = (value) => { try { return new URL(value, location.href).href; } catch (_) { return value || ''; } }; const imageUrl = (img) => { if (!img) return ''; const values = [img.getAttribute('data-lazy-src'), img.getAttribute('data-src'), img.getAttribute('data-original'), img.getAttribute('data-wpsrc'), img.currentSrc, img.getAttribute('src')]; const value = values.find((item) => item && !item.startsWith('data:') && !item.startsWith('blob:')) || ''; return absolute(value); }; for (const box of document.querySelectorAll('article')) { const a = Array.from(box.getElementsByTagName('a')).find((item) => item.href && item.href.includes('/video/')); if (!a) continue; const href = absolute(a.getAttribute('href') || a.href); if (!href) continue; const img = a.querySelector('img') || box.querySelector('img'); const heading = box.querySelector('.entry-title,h1,h2,h3,h4,.title'); const title = (a.getAttribute('title') || (img && img.getAttribute('alt')) || (heading && heading.textContent) || '').trim(); if (!title) continue; const duration = box.querySelector('.duration'); const item = { vod_id: href, vod_name: title, vod_pic: imageUrl(img), vod_remarks: duration ? duration.textContent.trim() : '' }; const old = cards.get(href); if (!old || (!old.vod_pic && item.vod_pic)) cards.set(href, item); } return Array.from(cards.values()); })())`; const DETAIL_JS = `JSON.stringify((() => { const titleNode = document.querySelector('main h1,article h1,h1'); const title = titleNode ? titleNode.textContent.trim() : document.title; const imageMeta = Array.from(document.getElementsByTagName('meta')).find((item) => item.getAttribute('property') === 'og:image'); const imageNode = document.querySelector('.responsive-player img,article img.data-no-lazy,main img'); const image = (imageMeta && imageMeta.content) || (imageNode && (imageNode.getAttribute('data-lazy-src') || imageNode.getAttribute('data-wpsrc') || imageNode.currentSrc || imageNode.src)) || ''; const infoRoot = document.querySelector('#video-infos,.video-description,article .entry-content,main') || document.body; const content = infoRoot ? infoRoot.innerText.trim().slice(0, 5000) : ''; return { title, image, content }; })())`; function base() { const value = String(config.baseUrl || 'https://www.bestjavporn.com'); return value.endsWith('/') ? value.slice(0, -1) : value; } function web(url, webJs) { const response = req(url, { webView: true, delayMs: 1000, blockImages: true, webJs, headers: { Referer: base() + '/' } }); const raw = response && (response.content || response.body); if (!raw) return null; try { return JSON.parse(raw); } catch (_) { return null; } } function pageUrl(url, page) { if (Number(page) <= 1) return url; const value = String(url); return (value.endsWith('/') ? value.slice(0, -1) : value) + '/page/' + page + '/'; } function list(url, page) { const currentPage = Number(page) || 1; const items = web(pageUrl(url, currentPage), LIST_JS) || []; return { page: currentPage, pagecount: items.length ? currentPage + 1 : currentPage, limit: items.length, total: items.length, list: items }; } return { init(ext) { config = ext && ext.ext ? ext.ext : (ext || {}); }, home() { return { class: [ { type_id: base() + '/v24/category/censored/', type_name: '有码' }, { type_id: base() + '/v6/category/uncensored/', type_name: '无码' }, { type_id: base() + '/v6/category/amateur/', type_name: '素人' }, { type_id: base() + '/v17/category/decensored/', type_name: '无码破解' }, { type_id: base() + '/', type_name: '首页' } ] }; }, homeVod() { return list(base() + '/', 1); }, category(tid, pg) { return list(String(tid || base() + '/'), Number(pg) || 1); }, search(wd, quick, pg) { const page = Number(pg) || 1; const root = page <= 1 ? base() + '/?s=' + encodeURIComponent(wd) : base() + '/page/' + page + '/?s=' + encodeURIComponent(wd); const items = web(root, LIST_JS) || []; return { page, pagecount: items.length ? page + 1 : page, limit: items.length, total: items.length, list: items }; }, detail(id) { const url = String(id); const data = web(url, DETAIL_JS) || {}; const title = data.title || url.split('/').filter(Boolean).pop() || 'BestJavPorn'; return { list: [ { vod_id: url, vod_name: title, vod_pic: data.image || '', vod_content: data.content || '', vod_play_from: '网页嗅探', vod_play_url: '播放$' + url } ] }; }, play(flag, id) { const url = String(id); return { parse: 1, url, click: '.responsive-player .play-button', header: { Referer: url } }; } }; })();"
      }
    }
  ]
}
